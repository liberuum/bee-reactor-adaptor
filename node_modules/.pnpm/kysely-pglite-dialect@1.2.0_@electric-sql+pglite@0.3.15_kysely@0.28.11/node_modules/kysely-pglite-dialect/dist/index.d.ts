import { Dialect, PostgresAdapter, Driver, QueryCompiler, Kysely, DatabaseIntrospector } from 'kysely';
import { PGlite } from '@electric-sql/pglite';
import { PGliteWorker } from '@electric-sql/pglite/worker';

declare class PGliteDialect implements Dialect {
    private readonly pgLite;
    constructor(pgLite: PGlite | PGliteWorker);
    createAdapter(): PostgresAdapter;
    createDriver(): Driver;
    createQueryCompiler(): QueryCompiler;
    createIntrospector(db: Kysely<any>): DatabaseIntrospector;
}

export { PGliteDialect };
